<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">

        <div class="form p-10 texy-white">
            <form action="/create" method="post">
                <input class="font-bold rounded-md outline-none block text-white w-full px-5 py-2 bg-zinc-700" type="text" placeholder="Title here" name="title">
                <textarea class="block text-white rounded-md outline-none w-full mt-2 px-5 py-2 resize-none bg-zinc-700" placeholder="Write your task details here" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md font-semibold hover:bg-blue-700" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
             <% if(files.length>0){ %>
                <% files.forEach((val)=>{  %>
                    <div class="task min- w-72 px-3 py-4 bg-zinc-800 rounded-md" >
                        <h1 class="text-white text-2xl tracking-tighter"><%= val%></h1>

                        <div class="mt-3 flex w-full justify-between items-center  ">
                             <a  class="text-blue-500 inline-block " href="/file/<%= val%>">Read More...</a>
                             <a  class="text-zinc-500 " href="/edit/<%=val%>">Edit Filename</a>
                        </div>
                       
                    </div>
               <% }) %>
            <% }else{ %>
                <h3 class="text-zinc-500">No Tasks Yet</h3>

          <%  } %>
      
            
        </div>
    </div>
    
</body>
</html>